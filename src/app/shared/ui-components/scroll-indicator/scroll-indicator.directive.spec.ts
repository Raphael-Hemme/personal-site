import { ScrollIndicatorDirective } from './scroll-indicator.directive';

describe('ScrollIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
